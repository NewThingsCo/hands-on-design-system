import { Meta, Typeset, Story } from '@storybook/addon-docs/blocks';
import { shadow } from "./tokens";

<Meta title="Shadows" />

# Shadows

<div>
{Object.entries(shadow).map((e, key) => {
  return (
    <div key={key}>
      <div style={{
        backgroundColor: e[1],
        display: "inline-block",
        height: "1em",
        margin: "0 2em 0",
        width: "1em"
      }}></div>
      <code>{e[0]}</code>
    </div>
  )
  console.log(e)
  })}
</div>
